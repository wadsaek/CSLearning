using System;
using Unit4.CollectionsLib;
using Library;

namespace _20._01._2025;

class Program {
    static void Main(string[] args) {

    }

    //static void Group<T>(Node<T> node, Func<T, T, bool> areGrouped) {
    //    Node<T> pointer = node;
    //    Node<T> groupExamples = new Node<T>(node.GetInfo());
    //    while (pointer is not null) {
    //        if (areGrouped(pointer.GetInfo(), pointer.GetNext().GetInfo())) {
    //            pointer = pointer.GetNext();
    //            continue;
    //        }
    //
    //        if (groupExamples.Contains((x) => areGrouped(
    //                        x.GetInfo(),
    //                        pointer.GetNext().GetInfo()
    //                    ))) {
    //
    //        }
    //    }
    //}

    //static bool UniqueGroups(Node<int> node, int k) {
    //    int len = node.LengthToNull();
    //    if (len % k != 0) return false;

    //    Node<int>[] arr = new Node<int>[k / 4];
    //    Node<int>[] tails = new Node<int>[arr.Length];
    //    for (int counter = 0; node is not null; node = node.GetNext()) {
    //        if(arr[counter/4] is null){

    //        } 
    //    }
    //}


}
